# Operator Portal

A modular Vue 3 Single Page Application built with TypeScript, Vite, and Tailwind CSS.

## Getting Started
To get the project running locally, simply run:
```bash
chmod +x setup.sh
./setup.sh